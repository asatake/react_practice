import React, { Component } from "react";
import { Container, Header, Message } from "semantic-ui-react";

class Sounds extends Component {
  render() {
    return (
      <Container>
        <Header as="h1">Sounds</Header>
        <Message>準備中</Message>
      </Container>
    );
  }
}

export default Sounds;
